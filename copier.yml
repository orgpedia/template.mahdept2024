# questions
dept_name:
    type: str
    help: What is the full deparment Name ? Home Department, Public Works Department

org_code:
    type: str
    help: What is the abbreviated name of this organization (one word) ? ex mahedu, mahhome

_exclude:
   - \.git
   - copier\.yml
